/// Layout breakpoints (aligned with `.cursorrules` `study_layout_breakpoint_width_lp`).
abstract final class NexusBreakpoints {
  /// At or above: multi-panel “wide” shell (PRD §7.5).
  static const double wideLayoutMinWidthLp = 840;
}
