name: nexus_lingua
description: Nexus Lingua — Universal Vocabulary Engine (Web-first prototype).
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.3

dependencies:
  flutter:
    sdk: flutter
  meta: ^1.15.0
  cupertino_icons: ^1.0.8
  sqflite: ^2.4.2
  sqflite_common_ffi_web: ^1.0.1
  path: ^1.9.1
  path_provider: ^2.1.5
  google_fonts: ^6.2.1
  shared_preferences: ^2.3.5
  file_picker: ^10.3.10
  csv: ^8.0.0
  excel: ^4.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  sqflite_common_ffi: ^2.3.6

flutter:
  uses-material-design: true
